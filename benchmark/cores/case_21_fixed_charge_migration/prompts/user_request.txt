Select one package for each of the <group_count> component groups.

The public group handles, in the zero-based order used by windows and pages, are:

<group_handles_text>

Use the costed sources strategically. Then call `submit_portfolio` exactly once
with the complete package list.
